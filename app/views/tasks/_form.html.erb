<%= form_with(model: task) do |form| %>
  <% if task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
        <% task.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, required: true  %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, required: true %>
  </div>

  <%# ¿Usamos select o usamos el calendario...? %>
  <div class="field">
    <%= form.label :due_date, 'Fecha' %>
    <%= form.date_select :due_date, placeholder: 'Ingresa tu fecha de nacimiento', selected: Date.today, required: true  %>
  </div>

  <div class="field">
    <%= form.label :priority %>
    <%= form.select :priority, [["Baja", "low"], ["Media", "medium"], ["Alta", "high"]], {} %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
